# Note that all program checks that fail using the data in this file generate warnings only, not hard errors.
# The JDK.MIN.VERSION property specifies the minimum JDK version appropriate for this product directory
# or ORACLE HOME.  This is the default property, used in the absence of a vendor-specific minimum version.
# The property should be in capital letters.
JDK.MIN.VERSION=1.6.0_03

# The JDK.MIN.VERSION.<VENDOR>SUN property specifies the minimum vendor-specific JDK version appropriate
# for this product directory or ORACLE HOME.
JDK.MIN.VERSION.SUN=1.6.0_11
JDK.MIN.VERSION.IBM=1.6.0_00